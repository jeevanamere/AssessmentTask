public class Rectangle { 
    integer length;
    integer breadth;
    public void input(Integer len, Integer bth){
        length = len;
        breadth = bth;
    }
        public void output(){
        System.debug('length= '+ length);
        System.debug('breadth= '+ breadth); 
}
}